## **課題**

**問題文：**
`/product/:id` にアクセスしたときに、

- `id` が数値でない場合は「Invalid Product ID」と表示
- `id` が存在する場合は対応する商品名を表示
  するように実装してください。

**条件：**
(1) `parseInt` で `id` の数値変換を行うこと
(2) 無効な `id` の場合、赤文字で警告を表示すること
(3) TailwindCSSの `text-red-600` クラスを使用すること
